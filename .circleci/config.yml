version: 2.0
#Test circleCI auto buid
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run:
          name: "test docker-compose"
          command: |
            docker-compose up -d
            curl localhost:8080
