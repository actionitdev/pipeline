version: 2.0
jobs:
  build:
    machine:
      image: ubuntu-2004:202010-01
    steps:
      - checkout
      - run:
          name: "test docker-compose build"
          command: docker-compose up -d
      - run: sleep 100
      - run:
          name: "test database connection"
          command: docker exec $(docker ps -f name=mysql -q) mysql -u root -p$mysql_root_pw
      - run:
          name: "test website avaliability"
          command: |
            res=`curl localhost:8080`

            if [ "$?" -ne 0 ]
            then
              exit 1
            fi

            if [ "$res" = "404 page not found" ]
            then
              exit 1
            fi
  deploy:
    machine:
      image: ubuntu-2004:202010-01
    steps:
      - run:
          name: lightsail stage server delploy  
          command: |
            ssh -i ./ai-sandbox.pem ubuntu@52.63.82.106
            "echo deploy starting && \
            cd wordpress && \
            git pull orgin dev && \ 
            docker-compose up -d && \
            echo depoly finished"


workflows:
  version: 2
  build-and-deploy:
    jobs:
      - build
      - deploy:
          requires:
            - build
          filters:
            branches:
              only: dbBackup

